// <auto-generated/>
using System;
using System.Buffers;
using System.Buffers.Binary;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using FourSer.Contracts;
using System.IO;
using FourSer.Gen.Helpers;
using SpanReader = FourSer.Gen.Helpers.RoSpanReaderHelpers;
using StreamReader = FourSer.Gen.Helpers.StreamReaderHelpers;
using SpanWriter = FourSer.Gen.Helpers.SpanWriterHelpers;
using StreamWriter = FourSer.Gen.Helpers.StreamWriterHelpers;

namespace FourSer.Tests.Records;

public partial record class RecordClass : ISerializable<RecordClass>
{
    public static int GetPacketSize(RecordClass obj)
    {
        if (obj is null)
        {
            throw new System.ArgumentNullException(nameof(obj));
        }
        var size = 0;
        size += 4;
        size += StringEx.MeasureSize(obj.B);
        return size;
    }

    public static RecordClass Deserialize(ref System.ReadOnlySpan<byte> buffer)
    {
        int a;
        string b;
        a = SpanReader.ReadInt32(ref buffer);
        b = SpanReader.ReadString(ref buffer);
        var obj = new RecordClass(a, b);
        return obj;
    }

    public static RecordClass Deserialize(System.ReadOnlySpan<byte> buffer)
    {
        return Deserialize(ref buffer);
    }

    public static RecordClass Deserialize(System.IO.Stream stream)
    {
        int a;
        string b;
        a = StreamReader.ReadInt32(stream);
        b = StreamReader.ReadString(stream);
        var obj = new RecordClass(a, b);
        return obj;
    }

    public static void Serialize(RecordClass obj, ref System.Span<byte> data)
    {
        if (obj is null)
        {
            throw new System.ArgumentNullException(nameof(obj));
        }
        SpanWriter.WriteInt32(ref data, (int)(obj.A));
        SpanWriter.WriteString(ref data, obj.B);
    }

    public static void Serialize(RecordClass obj, System.Span<byte> data)
    {
        Serialize(obj, ref data);
    }

    public static void Serialize(RecordClass obj, System.IO.Stream stream)
    {
        if (obj is null)
        {
            throw new System.ArgumentNullException(nameof(obj));
        }
        StreamWriter.WriteInt32(stream, (int)(obj.A));
        if (string.IsNullOrEmpty(obj.B))
        {
            stream.WriteInt32(0);
        }
        else
        {
            var bStringByteCount = System.Text.Encoding.UTF8.GetByteCount(obj.B);
            var bStringTotalBytes = bStringByteCount + sizeof(int);
            if (bStringTotalBytes <= 256)
            {
                Span<byte> bStringBuffer = stackalloc byte[bStringTotalBytes];
                System.Buffers.Binary.BinaryPrimitives.WriteInt32LittleEndian(bStringBuffer, bStringByteCount);
                var bStringActualBytes = System.Text.Encoding.UTF8.GetBytes(obj.B, bStringBuffer.Slice(sizeof(int)));
                stream.Write(bStringBuffer.Slice(0, sizeof(int) + bStringActualBytes));
            }
            else
            {
                var bStringBufferRented = System.Buffers.ArrayPool<byte>.Shared.Rent(bStringTotalBytes);
                try
                {
                    var bStringBuffer = bStringBufferRented.AsSpan(0, bStringTotalBytes);
                    System.Buffers.Binary.BinaryPrimitives.WriteInt32LittleEndian(bStringBuffer, bStringByteCount);
                    var bStringActualBytes = System.Text.Encoding.UTF8.GetBytes(obj.B, bStringBuffer.Slice(sizeof(int)));
                    stream.Write(bStringBuffer.Slice(0, sizeof(int) + bStringActualBytes));
                }
                finally
                {
                    System.Buffers.ArrayPool<byte>.Shared.Return(bStringBufferRented);
                }
            }
        }
    }
}


// <auto-generated/>
using System;
using System.Buffers;
using System.Buffers.Binary;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using FourSer.Contracts;
using System.IO;
using FourSer.Gen.Helpers;
using SpanReader = FourSer.Gen.Helpers.RoSpanReaderHelpers;
using StreamReader = FourSer.Gen.Helpers.StreamReaderHelpers;
using SpanWriter = FourSer.Gen.Helpers.SpanWriterHelpers;
using StreamWriter = FourSer.Gen.Helpers.StreamWriterHelpers;

namespace FourSer.Tests.Records;

public partial record struct RecordStruct : ISerializable<RecordStruct>
{
    public static int GetPacketSize(RecordStruct obj)
    {
        var size = 0;
        size += 4;
        size += StringEx.MeasureSize(obj.D);
        return size;
    }

    public static RecordStruct Deserialize(ref System.ReadOnlySpan<byte> buffer)
    {
        int c;
        string d;
        c = SpanReader.ReadInt32(ref buffer);
        d = SpanReader.ReadString(ref buffer);
        var obj = new RecordStruct(c, d);
        return obj;
    }

    public static RecordStruct Deserialize(System.ReadOnlySpan<byte> buffer)
    {
        return Deserialize(ref buffer);
    }

    public static RecordStruct Deserialize(System.IO.Stream stream)
    {
        int c;
        string d;
        c = StreamReader.ReadInt32(stream);
        d = StreamReader.ReadString(stream);
        var obj = new RecordStruct(c, d);
        return obj;
    }

    public static void Serialize(RecordStruct obj, ref System.Span<byte> data)
    {
        SpanWriter.WriteInt32(ref data, (int)(obj.C));
        SpanWriter.WriteString(ref data, obj.D);
    }

    public static void Serialize(RecordStruct obj, System.Span<byte> data)
    {
        Serialize(obj, ref data);
    }

    public static void Serialize(RecordStruct obj, System.IO.Stream stream)
    {
        StreamWriter.WriteInt32(stream, (int)(obj.C));
        if (string.IsNullOrEmpty(obj.D))
        {
            stream.WriteInt32(0);
        }
        else
        {
            var dStringByteCount = System.Text.Encoding.UTF8.GetByteCount(obj.D);
            var dStringTotalBytes = dStringByteCount + sizeof(int);
            if (dStringTotalBytes <= 256)
            {
                Span<byte> dStringBuffer = stackalloc byte[dStringTotalBytes];
                System.Buffers.Binary.BinaryPrimitives.WriteInt32LittleEndian(dStringBuffer, dStringByteCount);
                var dStringActualBytes = System.Text.Encoding.UTF8.GetBytes(obj.D, dStringBuffer.Slice(sizeof(int)));
                stream.Write(dStringBuffer.Slice(0, sizeof(int) + dStringActualBytes));
            }
            else
            {
                var dStringBufferRented = System.Buffers.ArrayPool<byte>.Shared.Rent(dStringTotalBytes);
                try
                {
                    var dStringBuffer = dStringBufferRented.AsSpan(0, dStringTotalBytes);
                    System.Buffers.Binary.BinaryPrimitives.WriteInt32LittleEndian(dStringBuffer, dStringByteCount);
                    var dStringActualBytes = System.Text.Encoding.UTF8.GetBytes(obj.D, dStringBuffer.Slice(sizeof(int)));
                    stream.Write(dStringBuffer.Slice(0, sizeof(int) + dStringActualBytes));
                }
                finally
                {
                    System.Buffers.ArrayPool<byte>.Shared.Return(dStringBufferRented);
                }
            }
        }
    }
}

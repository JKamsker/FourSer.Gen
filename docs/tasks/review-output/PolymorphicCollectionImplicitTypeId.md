# PolymorphicCollectionImplicitTypeId Review

## Verdict
No material functional findings. The generated code in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt` is functionally correct for `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/input.cs:9-13` and matches the likely expectation for an implicit `SingleTypeId` list: it writes one byte discriminator for the whole collection, rejects mixed item types before writing payload bytes, and deserializes every entry through the selected concrete type. The empty and `null` collection fallback to discriminator `10` is also consistent with existing `SingleTypeId` behavior in `tests/FourSer.Tests.Behavioural/Polymorphism/PolymorphicCollectionParityTests.cs:292-345`.

## Findings
No material correctness or expectation gaps found.

1. Info: The generated `Inventory` serializer repeats the same discriminator mapping and homogeneous-collection validation several times. `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt:32-50` does it in `GetPacketSize`, `:160-191` and `:260-291` repeat the validation loops in the span and stream serializers, and `:209-216` plus `:309-316` resolve the discriminator again for the write switch. This is not wrong, but it is duplicate generated logic.
2. Info: This snapshot emits four separate zero-payload serializers even though the test types carry no serialized state. `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/input.cs:16-33` marks `Item`, `Sword`, `Shield`, and `Potion` with `[GenerateSerializer]`, and the generated bodies at `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt:369-422`, `:443-496`, `:517-570`, and `:591-644` are nearly all boilerplate null checks and object construction.

## Performance opportunities
- Batch the collection prefix on both paths. The current code reads and writes the `int` count and `byte` type id as separate operations at `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt:68-70`, `:115-117`, `:195-196`, `:203-204`, `:208-217`, `:295-296`, `:303-304`, and `:308-317`. A single fixed-width prefix write or read would trim helper calls and stream I/O boundaries.
- Reuse the already-validated discriminator instead of recomputing it from the first item during serialization. The second switch at `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt:209-216` and `:309-316` is pure duplicate work after `:167-189` and `:267-289`.
- Special-case zero-sized concrete item serializers for this test shape. `Sword`, `Shield`, and `Potion` all have `GetPacketSize(...) == 0` and serialization bodies that only null-check at `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt:445-452`, `:476-494`, `:519-526`, `:550-568`, `:593-600`, and `:624-642`, but `Inventory` still loops every item in `GetPacketSize` and both serialize methods at `:34-50`, `:222-240`, and `:322-340`. For empty marker-like subtypes, those per-item calls are overhead with no payload bytes produced.

## Open questions / assumptions
- Assuming current intended behavior is that `SingleTypeId` collections without an explicit default discriminator use the first declared `[PolymorphicOption]`, the default `10` written for empty or `null` `Items` at `tests/FourSer.Tests/GeneratorTestCases/PolymorphicCollectionImplicitTypeId/PolymorphicCollectionImplicitTypeId.RunGeneratorTest.verified.txt:195-196`, `:203-204`, `:295-296`, and `:303-304` is correct.
- This review is based on source inspection only. I did not run generator or behavioral tests because the task limited writes to this report file.

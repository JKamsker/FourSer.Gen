# PolymorphicIndividualTypeIds Review

## Verdict
Functionally correct and aligned with likely user expectations for this test case. `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/input.cs:6-13` asks for an `IndividualTypeIds` collection with `byte` discriminators over `List<Animal>`, and the generated code writes a byte count, then a byte type id for each item, then dispatches to `Cat` or `Dog` while preserving the inherited `Animal.Id` plus subtype-specific fields in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/PolymorphicIndividualTypeIds.RunGeneratorTest.verified.txt:28-40`, `:55-117`, `:120-199`, `:245-323`, and `:369-447`. That matches the heterogeneous-collection contract documented in `src/FourSer.Contracts/SerializeCollectionAttribute.cs:22-34` and the existing mixed-type round-trip coverage in `tests/FourSer.Tests.Behavioural/UseCases/IndividualTypeIdsTests.cs:12-47`. No material functional or expectation-mismatch findings.

## Findings
No material findings.

1. Low: The snapshot duplicates the fused UTF-8 stream-string write block across subtype serializers. `Cat.Serialize(Stream)` and `Dog.Serialize(Stream)` emit near-identical logic in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/PolymorphicIndividualTypeIds.RunGeneratorTest.verified.txt:285-323` and `:409-447`. This is intentional under the default optimization level, as shown by `tests/FourSer.Tests/GeneratorTests.cs:722-763`, so it is not a correctness issue. It is still redundant generated code relative to the shared helper implementation in `src/FourSer.Gen/Resources/Code/StreamWriterHelpers.cs:120-155`.

## Performance opportunities
- No major missed batching gap is visible in this snapshot. The stream path already fuses string length and payload writes inside the subtype serializers in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/PolymorphicIndividualTypeIds.RunGeneratorTest.verified.txt:285-323` and `:409-447`, which is the main stream/IO optimization in this case.
- Minor future opportunity: `PolymorphicIndividualTest.Serialize(Stream)` writes each item discriminator as its own `WriteByte` call before entering the nested subtype serializer in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/PolymorphicIndividualTypeIds.RunGeneratorTest.verified.txt:180-189`. If polymorphic fixed headers are ever batched, the discriminator byte could be packed together with the fixed-size `Id` field that immediately follows in `:291-305` and `:415-429`, reducing one small stream write per item before the string payload begins.
- Similar read-side opportunity: `Deserialize(Stream)` reads the discriminator and then the subtype's `Id` in separate operations in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/PolymorphicIndividualTypeIds.RunGeneratorTest.verified.txt:98-108`, `:264-265`, and `:388-389`. A batched fixed-prefix read per item would reduce syscall pressure for large heterogeneous collections, although the current code is straightforward and correct.

## Open questions / assumptions
- Assumed the current collection contract intentionally canonicalizes `null` collections to zero-count and deserializes that back to an empty collection. This case's source member is non-nullable and initialized with `new()` in `tests/FourSer.Tests/GeneratorTestCases/PolymorphicIndividualTypeIds/input.cs:13`, so that behavior does not appear to conflict with the fixture's intent.
- I could not complete a local `dotnet test` run because the environment is missing `Microsoft.NETCore.App 9.0.0` for the test host. The verdict is therefore based on static inspection plus the existing in-repo behavioral coverage, not on a completed xUnit execution.

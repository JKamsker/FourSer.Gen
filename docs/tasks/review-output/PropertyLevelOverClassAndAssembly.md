# PropertyLevelOverClassAndAssembly Review

## Verdict
Functionally correct and aligned with likely user expectations. The input applies `[Serializer(typeof(PropertyStringSerializer))]` to `Packet.Name`, so the property-level serializer should override both the class-level and assembly-level `DefaultSerializer` entries declared in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/input.cs:5,10,13-14`. The generated `GetPacketSize`, span deserialization, stream deserialization, span serialization, and stream serialization paths all route through `PropertyStringSerializer` in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:28,44,57,68-69,83`. That matches the documented override model in `README.md:458-499` and the generator resolution logic in `src/FourSer.Gen/TypeInfoProvider.cs:583-621` plus `src/FourSer.Gen/CodeGenerators/Core/GeneratorUtilities.cs:165-198`.

No material correctness findings.

## Findings
- Low: The generated serializer cache contains an unused `ClassStringSerializer` instance in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:100`. Nothing in the generated `Packet` implementation references that field; every generated call site uses `PropertyStringSerializer` instead in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:28,44,57,68,83`. This is redundant generated code and a mild expectation mismatch, because a user would reasonably expect the overridden class-level serializer to disappear from the output. The redundancy comes from `src/FourSer.Gen/SerializerGenerator.cs:86-104`, which adds all type-level defaults to the global serializer cache even when a member-level serializer wins.
- Low: The generated file still emits the full shared import/alias header even though this case uses none of the helper aliases and only needs a small subset of the imports. `System.Buffers`, `System.Buffers.Binary`, `System.Collections.Generic`, `System.Linq`, `System.Runtime.CompilerServices`, `System.Text`, `FourSer.Gen.Helpers`, `SpanReader`, `StreamReader`, `SpanWriter`, and `StreamWriter` are redundant in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:3-15`. This comes from the unconditional header template in `src/FourSer.Gen/SerializerGenerator.cs:294-310`.

## Performance opportunities
- Removing the unused `ClassStringSerializer` cache field in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:100` would avoid one unnecessary static allocation and any constructor side effects for that serializer type.
- No meaningful batching or stream/IO reduction opportunity is visible in the live serialization path for this fixture. The packet has one member, and every real operation already collapses to a single `PropertyStringSerializer` call in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:28,44,57,68,83`. Resolved custom/default serializer members are also intentionally excluded from batching in `src/FourSer.Gen/CodeGenerators/Core/BatchingUtilities.cs:122-124` and `src/FourSer.Gen/CodeGenerators/Core/BatchingUtilities.cs:229-230`.
- Trimming the unused header entries in `tests/FourSer.Tests/GeneratorTestCases/PropertyLevelOverClassAndAssembly/PropertyLevelOverClassAndAssembly.RunGeneratorTest.verified.txt:2-15` would slightly reduce generated source size and Roslyn parse work, but it would not materially affect runtime behavior.

## Open questions / assumptions
- Assumption: the intended precedence is property-level `[Serializer]` over class-level `[DefaultSerializer]`, and class-level `[DefaultSerializer]` over assembly-level `[DefaultSerializer]`. That assumption matches `README.md:458-499`, `src/FourSer.Gen/TypeInfoProvider.cs:583-621`, and `src/FourSer.Gen/CodeGenerators/Core/GeneratorUtilities.cs:165-198`.
- I could not complete `dotnet test tests/FourSer.Tests/FourSer.Tests.csproj --filter "RunGeneratorTest(testCaseName=PropertyLevelOverClassAndAssembly)"` locally because `testhost.exe` requires `Microsoft.NETCore.App 9.0.0`, which is not installed on this machine. The review therefore relies on source inspection and the successful build portion of that command, not a completed xUnit execution.
